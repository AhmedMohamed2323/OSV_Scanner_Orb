description: >
  This command will echo orbs.
# What will this command do?
# Descriptions should be short, simple, and clear.
parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - run:
          name: Scan for OS Vulnerabilities 
          command: |
            osv-scanner -- .
