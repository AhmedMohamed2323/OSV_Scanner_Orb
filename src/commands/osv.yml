description: >
  This command will echo orbssssssssssssssssss.
# What will this command do?
# Descriptions should be short, simple, and clearrr.
parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - run:
      environment:
        PARAM_TO: <<parameters.to>>
      name: Hello Greeting
      command: <<include(scripts/greet.sh)>>