description: Install on OS Scanner on Windows

steps:
  - run:
      name:
      command: "Write-Host 'Hello, Windows'"

  - run:
      name: find working dir
      command: pwd

  - run:
      name: list files
      command: ls

  - run:
      name: OSV Scanner Download
      shell: pwsh
      command: |
        C:\Users\circleci\project\src\scripts\osv-scanner-install.ps1

