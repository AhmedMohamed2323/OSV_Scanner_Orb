description: |
  Install OSV Scanner
parameters:
  install-dir:
    default: /bin
    type: string
  version:
    default: 1.5.2
    description: Install OSV Scanner
    type: string
steps:
  - run:
      command: >
        wget -O osv_scanner https://github.com/google/osv-scanner/releases/download/v1.2.0/osv-scanner_1.2.0_linux_amd64
